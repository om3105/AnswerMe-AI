"""
API v1 Endpoints Package

Each module in this package is a self-contained set of related routes:
    auth.py   → Registration, login, token refresh, current user
    health.py → Liveness, readiness, and detailed health probes
    users.py  → User management (future: CRUD, roles, permissions)
"""
