"""
Health Check Schemas — Response Models

Classes:
    HealthResponse   → Basic health status response
    ReadinessResponse → Includes component-level status (DB, Redis)
    DetailedHealth    → Full system info (uptime, version, memory, connections)
"""
