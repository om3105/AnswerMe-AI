# Frontend (React + Vite)
# This directory will be initialized with React + Vite + TypeScript in Step 7.
# Using Vite as the build tool for lightning-fast HMR and optimized builds.
# No server-side rendering — pure client-side SPA.
